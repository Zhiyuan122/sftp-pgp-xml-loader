# SFTP Configuration - Production Environment
sftp.host=sftp.prod.quikcard.com
sftp.port=3512
sftp.username=Telus_CDAProvFile01
sftp.password=Abb2&aa8U
# Alternative: use private key authentication
# sftp.private.key.path=/path/to/private/key
# sftp.private.key.passphrase=your_passphrase

# SFTP Directories
sftp.remote.directory=C:\sFTPRoot\CDA_Provider_Files
sftp.local.inbox.directory=./inbox

# Known hosts file for SFTP security
sftp.known.hosts.path=~/.ssh/known_hosts

# File patterns
file.pattern.xml=*.xml

# PGP Configuration (for future use)
pgp.private.key.path=/path/to/pgp/private.key
pgp.private.key.passphrase=your_pgp_passphrase
pgp.public.key.path=/path/to/pgp/public.key

# Database Configuration (for future use)
db.url=jdbc:oracle:thin:@localhost:1521:xe
db.username=your_db_username
db.password=your_db_password
db.driver=oracle.jdbc.driver.OracleDriver

# Audit Configuration
audit.log.level=INFO
audit.log.file.path=./logs/audit.log

# Application Settings
app.retry.attempts=3
app.retry.delay.ms=1000
app.connection.timeout.ms=30000
